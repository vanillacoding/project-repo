import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  logoWrapper: {
    alignItems: 'center',
    marginTop: 20,
    flexDirection: 'row'
  },
  logoText: {
    color: 'white',
    fontSize: 20,
    fontWeight: 'bold',
    marginRight: 10,
    marginLeft: 10
  }
});

export default styles;
