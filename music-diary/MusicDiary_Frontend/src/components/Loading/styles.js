import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  animationContainer: {
    width: 400,
    height: 300,
    marginTop: "50%",
  },
});

export default styles;
