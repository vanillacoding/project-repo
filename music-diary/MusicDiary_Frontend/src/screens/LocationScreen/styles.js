import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: 80,
    height: 80,
  },
  animationContainer: {
    width: 300,
    height: 300,
    marginTop: "10%",
  },
});

export default styles;
